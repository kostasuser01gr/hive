{
  "lint": ["uv run --project core ruff check core/", "uv run --project core ruff check tools/"],
  "format": ["uv run --project core ruff format --check core/", "uv run --project core ruff format --check tools/"],
  "test_core": ["cd core && uv run pytest tests/ -v"],
  "test_tools": ["cd tools && uv run pytest tests/ -v"],
  "validate_exports": ["uv run python scripts/validate_exports.py"]
}
