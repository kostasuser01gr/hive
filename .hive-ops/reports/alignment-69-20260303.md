# Alignment Report: perf(ci): enable test concurrency with pytest-xdist

## 1) Policy Delta
- Upstream and Fork have matching LICENSE, CONTRIBUTING, SECURITY, and CoC files.
- No policy divergence introduced.

## 2) CI Delta
- Upstream runs pytest sequentially.
- Fork will now run pytest concurrently via `-n auto` to improve CI performance.

## 3) Superset Validation Plan
- `uv run --project core ruff check core/ tools/`
- `uv run --project core ruff format --check core/ tools/`
- `cd core && uv sync && uv run pytest -n auto tests/ -v`
- `cd tools && uv sync --extra dev && uv run pytest -n auto tests/ -v`

## 4) Evidence
- `make check` passed successfully.
- `make test` successfully initialized 10 workers using `pytest-xdist` and passed cleanly locally.